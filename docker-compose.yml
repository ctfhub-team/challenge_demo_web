version: "2"
services:
  web:
    build: .
    image: skill_web_command_execute_php_filter_complex
    ports:
      - "10080:80"
    environment:
      - FLAG=ctfhub{command_execute_php_filter_complex}
